远程桌面的键鼠事件本质原理是： 把本机的输入事件“翻译成一组可序列化的输入指令”，通过网络发给被控端，再由被控端调用系统级输入注入 API，把这些指令变成“真实的鼠标键盘动作”。 这样被控端上的任意应用都会像收到了本地键鼠一样响应。

下面按“采集 → 映射 → 传输 → 注入”把原理讲清楚，并对应到你项目里的实现。

## 1) 采集：从 UI 框架拿到鼠标/键盘事件
原理：控制端并不是直接读操作系统的全局输入，而是 在远程画面控件上监听输入事件 。

- 控件：远程画面是 Image （显示被控端帧）
- 采集来源：Avalonia 的输入事件系统
  - 鼠标： PointerMoved/PointerPressed/PointerReleased/PointerWheelChanged
  - 键盘： KeyDown/KeyUp + TextInput
	
## 2) 映射：把“本机控件坐标”换算成“被控端屏幕坐标”
原理：你在本机点击的是 Image 内部某个点，但远端实际屏幕是 RemoteWidth × RemoteHeight。而且 Image.Stretch="Uniform" 会产生黑边/留白，所以不能简单用比例缩放。
项目做法：
- 计算 scale = min(localW/remoteW, localH/remoteH)
- 计算内容区域大小 contentW/H 和偏移 offsetX/Y（黑边）
- 把本地点 p 减去 offset，得到内容区域坐标，再除以 scale 得到远端像素坐标
- 并对越界情况做 clamp/丢弃，保证不会发出非法坐标

这一步是“点击哪里就控制哪里”的关键。

## 3) 传输：把输入动作变成 gRPC 事件流（可序列化、可重放）
原理：把每个输入动作编码成一个消息（protobuf），通过网络发给对端。
项目使用：
- 框架：gRPC（Grpc.Net.Client / Grpc.Core），底层是 HTTP/2（h2c 明文）
- 方式：双向流 InputStream，控制端持续写入 RemoteInputEvent，被控端回 RemoteInputAck（你现在主要用来 drain，不做强依赖）
消息里最重要的是：
- Type：例如 mouseMove/mouseDown/mouseUp/mouseWheel/keyDown/keyUp/text
- Payload：坐标、按钮、滚轮 delta、vk、utf8 文本等
- Seq/TsMs：顺序号和时间戳（辅助调试/统计）

此外还做了两个“让体验丝滑”的关键工程技巧：
- 鼠标移动节流/合并 ：每 16ms 取最新坐标发一次（避免洪泛）
- 写入串行化 ：用 Channel<RemoteInputEvent> + 单发送循环保证 gRPC RequestStream.WriteAsync 不并发（避免乱序/流损坏，这也是你之前“黏住鼠标/后续点击失效”的根因之一）

## 4) 注入：被控端把事件变成系统级“真实输入”
原理：被控端不能只在自己程序里“模拟点击”，必须调用 OS 的输入注入接口，让 任何应用 都认为这是用户真实键鼠输入。

在 Windows 上你们用的是：
- Win32 API： SendInput （user32.dll）
- 鼠标移动：用绝对坐标模式 MOUSEEVENTF_ABSOLUTE | MOUSEEVENTF_VIRTUALDESK ，并用 GetSystemMetrics 的虚拟屏幕尺寸把像素映射到 0..65535
- 鼠标按键： LEFTDOWN/LEFTUP 、 RIGHTDOWN/RIGHTUP
- 滚轮： MOUSEEVENTF_WHEEL/HWHEEL
- 键盘 VK： KEYBDINPUT （keyDown/keyUp）
- 文本： KEYEVENTF_UNICODE （逐字符注入）

总结一句
远程键鼠控制原理就是典型的 “UI 输入采集 + 坐标映射 + 网络事件流 + OS 输入注入”。
只要“映射准确”和“事件顺序稳定”，体验就会接近本地；你们现在通过节流、串行写、丢帧低延迟等手段，把它做到了很顺滑